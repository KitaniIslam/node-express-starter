import { IUser } from "@/types/User.t.js";

/**
 * @param {string[]} payload (array of usernames)
 * @returns {IUser[]}
 */

export const getUserData = (payload: string[]) => {
  // TODO: read from database
  return "";
};

export const addUsers = () => {
  // TODO: write to database
  return "";
};
